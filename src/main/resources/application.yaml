spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/MedCore
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
    properties:
      hibernate.enable_lazy_load_no_trans: true
      hibernate:
        format_sql: true
logging:
  level:
    com.example: DEBUG
    root: INFO
  file:
    name: C:/Users/serez/IdeaProjects/MedCore/logs/app.log

